# Sentry Error Tracking Configuration
# Get your DSN from https://sentry.io/ after creating a project

# Required: Sentry DSN (Data Source Name) for error tracking
# If not set, Sentry will not be initialized
VITE_SENTRY_DSN=

# Optional: Environment name (defaults to 'production')
VITE_SENTRY_ENVIRONMENT=production

# Optional: Application version for release tracking
# Useful for tracking which version has issues
VITE_APP_VERSION=1.0.0

# Optional: Performance monitoring sample rate (0.0 to 1.0)
# Default is 0.1 (10% of transactions)
VITE_SENTRY_TRACES_SAMPLE_RATE=0.1

# Optional: Force Sentry initialization in development mode
# Normally Sentry is disabled in development
# VITE_SENTRY_FORCE_DEV=true

# Backend API URL (if different from default)
# VITE_API_URL=http://localhost:8000
